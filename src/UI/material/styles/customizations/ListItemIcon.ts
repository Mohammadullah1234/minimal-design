"use client";

import { SxProps, Theme } from "@/UI/styles/MuiStyles";

export function listItemIconStyles(): SxProps<Theme> {
  return {
    minWidth: "42px",
    color: "currentcolor",
    marginTop: "4px",
  };
}
