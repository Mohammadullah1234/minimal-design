import { LazyPageLoader } from "@/app/layout/minimalDashboard";

const BankingPage = () => {
  return (
    <LazyPageLoader>
      <h1>Welcome to BankingPage Page</h1>
    </LazyPageLoader>
  );
};

export default BankingPage;
