import { LazyPageLoader } from "@/app/layout/minimalDashboard";

const BlogListPage = () => {
  return (
    <LazyPageLoader>
      <h1>Welcome to BlogListPage Page</h1>
    </LazyPageLoader>
  );
};

export default BlogListPage;
