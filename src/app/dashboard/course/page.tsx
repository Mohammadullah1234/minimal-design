import { LazyPageLoader } from "@/app/layout/minimalDashboard";

const CoursePage = () => {
  return (
    <LazyPageLoader>
      <h1>Welcome to CoursePage Page</h1>
    </LazyPageLoader>
  );
};

export default CoursePage;
