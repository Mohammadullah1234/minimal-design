
import { LazyPageLoader } from "@/app/layout/minimalDashboard";

const FileManagerPage = () => {
  return (
    <LazyPageLoader>
      <h1>Welcome to File Manager Page</h1>
    </LazyPageLoader>
  );
};

export default FileManagerPage;
