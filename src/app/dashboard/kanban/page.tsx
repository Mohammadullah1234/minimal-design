import { LazyPageLoader } from "@/app/layout/minimalDashboard";

const Kanban_Page = () => {
  return (
    <LazyPageLoader>
      <h1>Welcome to Kanban_Page</h1>
    </LazyPageLoader>
  );
};

export default Kanban_Page;
