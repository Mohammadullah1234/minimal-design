import { LazyPageLoader } from "@/app/layout/minimalDashboard";

const OrderDetailsMain = () => {
  return (
    <LazyPageLoader>
      <h1>Welcome to OrderDetailsMain</h1>
    </LazyPageLoader>
  );
};

export default OrderDetailsMain;
