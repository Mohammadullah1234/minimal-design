
import { LazyPageLoader } from "@/app/layout/minimalDashboard";

const OrderListPage = () => {
  return (
    <LazyPageLoader>
      <h1>Welcome to OrderListPage Page</h1>
    </LazyPageLoader>
  );
};

export default OrderListPage;
