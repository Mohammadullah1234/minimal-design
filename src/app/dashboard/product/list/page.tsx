import { LazyPageLoader } from "@/app/layout/minimalDashboard";

const ProductListPage = () => {
  return (
    <LazyPageLoader>
      <h1>Welcome to ProductListPage Page</h1>
    </LazyPageLoader>
  );
};

export default ProductListPage;
