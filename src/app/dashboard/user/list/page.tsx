
import { LazyPageLoader } from "@/app/layout/minimalDashboard";

const UserListPage = () => {
  return (
    <LazyPageLoader>
      <h1>Welcome to UserListPage Page</h1>
    </LazyPageLoader>
  );
};

export default UserListPage;
